export type Service = {
  id: number;
  name: string;
  image: string;
  order: number;
  color: string;
};

export type Catalog = {
  id: number;
  name: string;
  image: string;
  order: number;
};
